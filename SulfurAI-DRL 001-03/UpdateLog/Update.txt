Please note only releases will be mentioned here.


13/04/2025 Release DRL 1-03-54b:

Whats new:
-Verified input and cleaned it.
-Added a maximum character limit for input.

-Bug fixes:
 *Check_device_s now uses a calling method to get input_data -> links back to the input verified.
 *Output now has a calling error system in beta.

-QoL changes:
 *Added this new UpdateLog folder.
 *Verified files in architecture for efficient verification.
 *Changes to output.

-SDK changes:
 *Calling input is used via a module and not with a separate script.
 *Improves the speed.